import { ShowFullDescriptionDirectiveDirective } from './show-full-description.directive.directive';

describe('ShowFullDescriptionDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowFullDescriptionDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
